import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  bottomMaxPadding: {
    position: 'relative',
    paddingBottom: 100,
  },
  bottomPadding: {
    position: 'relative',
    paddingBottom: 45,
  },
});

export default styles;